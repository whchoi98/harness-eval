# Environment
.env
*.local.json

# OS
.DS_Store
Thumbs.db

# Editor
*.swp
*.swo
*~
.idea/
.vscode/

# Python
__pycache__/
*.pyc
.venv/

# Node (if ever needed)
node_modules/

# Test artifacts
tests/tmp/
*.log

# Evaluation output (runtime artifacts written into target projects)
# The scripts write history, latest.json, and reports under .harness-eval/;
# an older layout used .harness-eval-history/. Ignore both so generated results
# (which may embed scanned paths/finding excerpts and are a badge.sh injection
# vector) are never committed by accident.
.harness-eval/
.harness-eval-history/
