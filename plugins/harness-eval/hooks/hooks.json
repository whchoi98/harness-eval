{
  "description": "harness-eval plugin hooks - post-evaluation badge generation (opt-in; README is modified only when HARNESS_EVAL_AUTO_BADGE=1 or .harness-eval/config.json autoBadge:true)",
  "hooks": {
    "Stop": [
      {
        "hooks": [
          {
            "type": "command",
            "command": "bash ${CLAUDE_PLUGIN_ROOT}/hooks/post-eval-badge.sh",
            "timeout": 30
          }
        ]
      }
    ]
  }
}
