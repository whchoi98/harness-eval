---
description: Compare harness evaluation history — current vs previous score/tier trends
allowed-tools: Read, Glob, Grep, Bash
---

Compare the current evaluation with the immediately preceding one from history.

Activate the `compare` skill from this plugin to analyze evaluation history: current-vs-previous score and per-tier deltas, trend/diminishing-returns detection, and next-grade projection.

<!-- Note: this command and the `compare` skill share the `harness-eval:compare` name (intentional thin-wrapper pattern). See skills/CLAUDE.md for the coexistence rationale. -->

