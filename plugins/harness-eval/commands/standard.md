---
description: Standard harness evaluation — static + dynamic analysis (~2-3min)
allowed-tools: Read, Glob, Grep, Bash
---

Run a standard harness evaluation on the current project.

Activate the `standard` skill from this plugin to perform static and dynamic analysis.

<!-- Note: this command and the `standard` skill share the `harness-eval:standard` name (intentional thin-wrapper pattern). See skills/CLAUDE.md for the coexistence rationale. -->

